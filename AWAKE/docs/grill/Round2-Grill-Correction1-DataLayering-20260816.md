1. Q: Schema: Does “compression must not touch transcript storage” coexist safely with “allow rolling unpinned originals after compression” when rolling is itself a transcript mutation? A: Split compression from a separate `awake.transcript.roll.v1` command and allow only that command to mutate transcript after audit.
2. Q: Schema: What does “read-only migration” mean operationally when old messenger rows must be read and copied into `awake.transcript.v1`? A: Define it as one-way migrate-from-old/copy-to-new with old data untouched and a durable migration status.
3. Q: Schema: Does the field list specify types, required/optional flags, enums, max lengths, and schema version for every transcript field? A: Add a versioned JSON schema with typed required fields and bounded limits for all nine fields.
4. Q: Schema: Is there a fixed source taxonomy for messenger, direct dialogue, scene shout, event, letter, and system rows? A: Define a finite `source` enum and reject unknown source values before append.
5. Q: Schema: Is there a fixed kind taxonomy separating player, NPC, system, failed, and hidden rows? A: Define finite `kind` values and reject unmarked or unsupported rows before persistence.
6. Q: Schema: Does each transcript row carry correlation/causation metadata that survives save/load for replay and debugging? A: Persist correlation/causation on the command envelope or a bounded row field.
7. Q: Schema: Is transcript `conversationId` allocated from the same source as `NpcMemoryService.Reserve`, so memory compression can trace raw rows back? A: Reuse the store reservation sequence and require raw rows to carry the same conversationId.
8. Q: Schema: Does the revised three-layer correction specify that relationship state remains command-derived and never sourced from raw transcript text? A: State relationship as `awake.relationship.state.v1` composed from validated delta commands only.
9. Q: Storage: What is the exact per-contact or per-chunk key scheme, including safe encoding of target IDs and chunk ordering? A: Specify `campaign.transcript.v1.<targetId>.<chunkIndex>` or equivalent with documented encoding and order.
10. Q: Storage: Does the 512KB limit account for schema, `updatedUtc`, `appliedKeys`, chunk metadata, and JSON escaping overhead? A: Define a hard byte guard that checks serialized chunk size before `SetAsync`.
11. Q: Storage: Are transcript appends append-only per chunk, or do they still rewrite the full messenger blob on every turn? A: Add per-contact append records or a compact append command to avoid repeated full-document read-modify-write.
12. Q: Storage: How are multi-chunk reads kept consistent if a chunk write fails midway? A: Treat each committed chunk as independently valid and rebuild history only from committed chunk records.
13. Q: Storage: Is the 256-entry global applied-keys ledger enough for idempotency across many contacts with 200 rows each? A: Move transcript dedupe metadata into transcript chunks/contact state instead of the shared 256-entry ledger.
14. Q: Storage: Does every transcript write use a stable per-line idempotency key and the same retry path as other `WorldStateStore` commands? A: Require per-line idempotency keys and drain retries before returning success.
15. Q: Storage: Are queued transcript writes durable before session end and before final drain releases the store? A: Enqueue through `WorldStateStore` and require `BeginFinalDrainAsync` to finish before release.
16. Q: Storage: Do transcript read/write/drain operations carry deadlines, cancellation tokens, and correlation IDs instead of `CancellationToken.None`? A: Add deadline/cancellation/correlation to all transcript async paths.
17. Q: UI: Does the revised plan remove the synchronous `LoadAsync(...).GetAwaiter().GetResult()` from messenger VM construction? A: Make history loading async with a loading state and lazy per-contact loading.
18. Q: UI: Are in-memory transcript caches reset and re-keyed on every campaign/session lifecycle change? A: Reset on CampaignSessionReady and key caches by session/timeline.
19. Q: UI: Does the panel present raw history and memory summaries as separate layers so a player can distinguish evidence from AI-derived memory? A: Keep raw transcript and `npc_memory` in separate tabs/cards with distinct provenance labels.
20. Q: UI: Does the current 100-row chat viewport avoid implying or enforcing the 200-line storage cap? A: Paginate or virtualize history so UI cap and storage retention are independent.
21. Q: UI: Is raw history gated by ContentPolicy so pure mode never displays optional/adult transcript rows? A: Filter or hide gated rows at load/display time and keep the runtime content-neutral.
22. Q: Performance: Is transcript compression bounded per day with a retry budget so it cannot stall the campaign tick? A: Use a per-day cap and scheduled budget similar to the existing memory consolidator.
23. Q: Performance: Is there a worst-case load test for many contacts/chunks under the framework 512KB and context limits? A: Add SdkSmoke coverage for worst-case chunk count, total bytes, and load latency.
24. Q: Performance: Do transcript operations reserve storage/UI budget so one oversized conversation cannot starve other contacts? A: Enforce per-contact caps and chunk limits independently of global state.
25. Q: Performance: Are per-contact writes serialized so UI, memory, and proactive paths cannot interleave or duplicate appends? A: Reuse `WorldStateStore` drain serialization plus per-line idempotency.
26. Q: Performance: Does the UI release history snapshots and stream buffers when switching contacts or closing the panel? A: Clear transient chat rows/stream buffers and avoid retaining full raw transcripts in ViewModels.
27. Q: Token cost: What exactly replaces `dialogue_history`, and what is its fixed maximum byte budget? A: Define `bounded_session_summary` with a fixed byte cap and no raw lines.
28. Q: Token cost: If AI summarization consumes raw transcript, does that violate the default cloud-export prohibition? A: Use local deterministic summarization/facts or explicitly gate raw-input summarization under CloudExportPolicy.None and ContentPolicy.
29. Q: Token cost: Does the prompt guarantee `npc_memory` and `retrieved_knowledge` survive when the session summary is long? A: Truncate session summary first and reserve fixed budgets for memory and knowledge.
30. Q: Token cost: Does the current truncation order still place session summary before memory and knowledge, risking priority inversion? A: Change truncation order so raw/session text is cut first and memory/knowledge budgets are reserved.
31. Q: Token cost: Is there a prompt-builder assertion that no raw transcript text appears in either direct or compiled output? A: Add a SdkSmoke/unit test that checks final prompt text for transcript row IDs/text.
32. Q: Security: What happens if a user explicitly enables cloud export for transcripts? A: Keep raw transcript non-exportable by default and require a distinct opt-in permission/classification before raw-backed export.
33. Q: Security: Can raw transcript leak into RAG/worldbook through the memory-summary path? A: Exclude transcript namespace from retrieval and only allow validated memory summaries into RAG.
34. Q: Security: Is raw transcript scoped to the local player/campaign and never exposed as shared world state? A: Keep it under the campaign namespace with player/session ownership and no cross-save sharing.
35. Q: Security: Are transcript-backed summary calls and raw panel loads protected by the existing content and cloud policies? A: Gate every transcript-backed call through ContentPolicy and CloudExportPolicy with fail-closed defaults.
36. Q: Validation: Are malformed stored transcript rows skipped with corruption logging instead of crashing or silently overwriting chunks? A: Validate on load and quarantine/skip malformed rows while preserving valid rows.
37. Q: Validation: Is there a strict validator for speaker role, text length, location, source, kind, id, and day before command creation? A: Add a transcript validator returning explicit `awake.transcript.invalid_*` codes.
38. Q: Validation: Are day values validated as nonnegative monotonic campaign days rather than wall clock or in-memory day? A: Reject missing/negative/non-integer day and derive rollover from saved state.
39. Q: Validation: Are unnamed NPC and scene-shout targets excluded or assigned durable non-per-contact transcript keys? A: Restrict per-contact transcripts to stable hero IDs and route scene shouts to a non-memory scene namespace.
40. Q: Save/load: What happens if migration fails midway, and can it retry without losing old messenger data? A: Make migration idempotent and resumable with old data untouched until the new transcript schema is fully created.
41. Q: Save/load: Can migrated idempotency keys collide with new transcript keys after schema upgrade? A: Prefix migrated keys by source schema so old and new IDs cannot collide.
42. Q: Save/load: What is the exact crash ordering for memory write, audit write, and transcript roll? A: Require durable memory write and audit before roll, and make roll retry idempotently with the same audit ID.
43. Q: Save/load: Does loading an older save risk deleting newer transcript rows because current-day rollover is computed from a cache? A: Roll from the saved timeline and never destructively delete based on a transient current-day cache.
44. Q: Save/load: Is there a lifecycle test that queued transcript lines flush, reload with identical IDs, and produce no duplicates after retry? A: Add a save/load smoke covering queue drain, reload, and idempotent retry.
45. Q: Save/load: Are transcript pending writes included in `BeginSessionEnd` so they are not dropped if the final drain is interrupted? A: Add transcript reservations/outbox to session-end enqueue and verify final drain completeness.
46. Q: Compatibility: Does the plan update `AiTaskConstants`, `AwakeStorageContract`, `WorldStateKind`, namespace list, manifest, and permission catalog together? A: Add `TranscriptNamespace`, `TranscriptKey`, `TranscriptSchema`, and `WorldStateKind.Transcript` in one contract change.
47. Q: Compatibility: Is the public version kept tied to acceptance while the transcript migration remains internal and additive? A: Do not bump the release version for an internal additive schema migration.
48. Q: Compatibility: Do old `awake.messenger.v1` saves remain readable until migration succeeds, and is the old schema still recognized? A: Keep the old schema recognized during migration and switch reads only after migration is durable.
49. Q: Simpler alternative: Was extending `awake.messenger.v1` with metadata considered instead of a new schema and migration risk? A: If no contract requirement forces a rename, extend the existing schema; otherwise keep the migration one-way and additive.
50. Q: Simpler alternative: Was dropping AI-based raw transcript summarization and keeping only facts/promises considered for v0.2? A: Accept that simpler option only if player history remains a local non-AI log with no cloud summarization requirement.
VERDICT: REVISE
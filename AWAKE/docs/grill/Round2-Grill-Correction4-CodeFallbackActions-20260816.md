1. [data/schema] Does the revision pin exact command IDs and schema versions or leave `awake.action.*` open-ended? A: Define `awake.action.give_gold.v1` and `awake.action.promise_request.v1` with explicit input/output schemas.
2. [data/schema] Does `awake.interactions.v1` define per-contact keying, schema marker, result status, and migration defaults? A: Lock a `schema/heroId/entries/appliedKeys` document per hero and derive missing fields deterministically.
3. [data/schema] Is a request modeled as a pending promise with an explicit state machine rather than a flavor event? A: Use `promiseId/status(pending|accepted|kept|broken|rejected)/text/day` and let AI create only pending.
4. [data/schema] Do promise records carry player/NPC obligor/obligee fields so one-sided obligations are rejected? A: Store `playerHeroId`, `targetHeroId`, and `obligor/obligee`, and validate them against the active session.
5. [data/schema] Does AI provide only bounded suggestion args while code injects current player/target/session? A: AI supplies `amount/reason` or `text/reason` only; identity and session are code-injected.
6. [data/schema] Does the correction reject mismatched schema versions at load and command submission? A: Exact-match registered schema versions and fail closed on unknown or legacy versions.
7. [data/schema] Is the relationship delta formula deterministic and capped, not merely promised as code-derived? A: Publish fixed gold/promise mapping with hard caps and reject model-supplied deltas.
8. [storage] Does `WorldStateStore` gain an Interaction kind/schema/applier, since current `WorldStateKind` has no such case? A: Add `WorldStateKind.Interaction`, `awake.interactions.v1`, and idempotent `EnsureInteractionShape/ApplyInteraction`.
9. [storage] Is `AwakeStorageContract` updated so interaction writes cannot bypass schema normalization? A: Register `awake.interactions.v1` and reject or normalize every write through the contract.
10. [storage] How is the 512KB framework value limit enforced as ledger entries grow? A: Cap active entries, archive resolved records, and assert serialized byte size before persist.
11. [storage] Is the 256-entry `appliedKeys` trim safe after many interactions in one save? A: Use a durable interaction-specific idempotency mechanism or a separated key cap so replay cannot happen.
12. [storage] Are resolved promises archived instead of silently dropped at cap? A: Move kept/broken/rejected entries to an archive key and keep pending entries active.
13. [storage] Is gold authoritative in Bannerlord save while AWAKE stores only audit/history? A: Make game mutation authoritative and `awake.interactions.v1` a compact audit with defined ordering.
14. [storage] Does the revision define compensation if gold is deducted but ledger or event write fails? A: Use an idempotent gold ledger with expected before/after and a compensation path before success is reported.
15. [storage] Does settlement publish a durable EventService event for memory and reactions? A: Emit one interaction event after native mutation and ledger settle; never trust AI text as an event source.
16. [storage] Is the active ledger cached per contact and reloaded only on mutation/day/contact change? A: Cache one contact document and invalidate on state changes, not every frame.
17. [storage] Does final drain include pending interaction writes with retry/hard-failure semantics? A: Enqueue through `WorldStateStore` and honor existing drain behavior before store release.
18. [UI] What happens to the current command/settled-facts/memory path when an action is pending? A: Defer `_settledFacts` and memory recording until confirmed settlement, not at AI turn completion.
19. [UI] Is the action strip shared by the unified hub and standalone dialogue overlays instead of one-off UI? A: Put the strip in the shared hub VM/prefab and expose the same command invocation to both overlays.
20. [UI] Are suggestions invalidated on new send, contact switch, session close, or save/load? A: Bind suggestion lifetime to `sessionToken/generation` and clear on every transition.
21. [UI] Does each confirmation click generate a fresh `confirmationId` so edits or double-clicks do not reuse AI turn idempotency? A: Derive bridge idempotency from confirmation ID plus typed args and disable the button while pending.
22. [UI] Are editable fields bounded and rebuilt as typed commands instead of forwarding AI JSON? A: Render fixed amount/reason fields, validate on submit, and construct `CommandRequest` in code.
23. [UI] Does confirmation preserve Escape/input restoration and block overlapping sends? A: Keep focus-layer behavior, cancel pending proposal on close, and block send until resolved.
24. [UI] Is the settlement result rendered as a code-generated line rather than streamed AI text? A: Append a ledger/status line after result and never treat AI reply as proof.
25. [performance] Does opening a contact use one bounded snapshot read rather than scanning heroes or inventories? A: Extend `PlayerSnapshotDto` with gold or add a bounded interaction snapshot and read once per open/day.
26. [performance] Is action eligibility computed on contact open/day change, not per frame or button render? A: Cache eligibility and invalidate on contact/day/mutation changes.
27. [performance] Are ledger eviction and archive operations done in the applier, not render loops? A: Keep cleanup in command application and UI reads O(1) over active entries.
28. [performance] Does give-gold settlement stay at one preflight read, one native mutation, and one ledger write? A: Sequence the three steps without repeated storage or game-state reads.
29. [performance] Are only pending promises and recent counts loaded into UI/prompt instead of the full ledger? A: Bound active entries and prompt block to fixed small sizes.
30. [token cost] Is the action-schema prompt block separately budgeted so it cannot crowd out memory/knowledge? A: Reserve a fixed action block and enforce it in `NpcDialoguePromptPipeline`.
31. [token cost] Does the output contract allow at most one action suggestion per turn with bounded args? A: Enforce one `command` object per turn and the existing 16KiB command cap.
32. [token cost] Are raw action results excluded from prompt injection and only summarized into memory? A: Keep raw results in transcript/ledger and inject derived summaries only.
33. [token cost] With items and trade deferred, does the prompt omit inventory/equipment/roster data entirely? A: Remove item context now and add it only when native/game-data support exists.
34. [security] Does the correction define a real session token, since current coordinator has only source/target strings? A: Add `sessionId/generation/sessionToken` and require it in action preflight and execute.
35. [security] Are AI-suggested target IDs rejected before UI render and again in adapters? A: Strip AI target fields and compare code-injected active target in validator, VM, preflight, and execute.
36. [security] Is player identity checked against `AwakeRuntime.CurrentHeroId`, not just a live `Hero.MainHero` reference? A: Compare stable player IDs at preflight and execute and reject mismatch.
37. [security] Is the snapshot token state-bearing with balance/session/target/day/deadline instead of the current args-only hash? A: Replace `SnapshotFromArguments` with a state-derived token and recheck it before mutation.
38. [security] Are command permissions requested only on player confirmation while AI suggestion performs no command permission request? A: AI path validates/renders only; `PermissionGate.EnsureAsync` runs in the click handler.
39. [security] Are both commands registered atomically in `AiTaskConstants.NewCommandIds`, `PermissionCatalog`, `CommandRiskPolicy`, and manifest with exact risk tiers? A: Add both IDs to all registries and assign give-gold R2 and promise R1/R2 in one change.
40. [security] Does strict schema validation reject unknown/duplicate properties, non-integer amounts, negative values, and amounts above cap/balance? A: Typed parse with allowlist and enforce `1 <= amount <= min(cap, snapshot.Gold)`.
41. [validation] Does every failure use typed `awake.action.*` codes with category/retry semantics and preserved owner/correlation? A: Define codes for insufficient gold, snapshot mismatch, target mismatch, denied permission, ledger full, and stale session.
42. [validation] Is balance revalidated at execution time, not only at render, so save/load or another mod cannot change state mid-flow? A: Verify current balance and session immediately before mutation against the snapshot token.
43. [validation] Are reason and identity fields bounded to safe text/identifier rules without parsing localized errors? A: Clamp reason to 240 chars, use existing identifier validation, and branch on typed codes.
44. [save/load] Does load detect a corrupt `awake.interactions.v1` document and fail closed without destructive recovery? A: Validate schema, log a typed corruption error, and refuse to open/replay that ledger.
45. [save/load] Does reload preserve gold mutation idempotency and compensation state rather than re-applying or losing a half-settled action? A: Persist gold ledger status and compensation markers before success and recover deterministically.
46. [save/load] Are existing `awake.npc.memories.promises` reconciled with the interaction ledger to prevent duplicate or lost promises? A: Choose one canonical source, migrate legacy promises once, and prevent dual writes.
47. [compatibility] Is the native/game-data API for reading and changing player gold verified on Bannerlord 1.3.15 and 1.4.8 before promising give-gold? A: Verify exact API/version behavior or defer give-gold until proven.
48. [compatibility] Does the design avoid depending on legacy Slaanesh altar code, AF state, or not-yet-enabled CapabilityBroker? A: Reuse only reviewed patterns and route all runtime logic through current AWAKE/framework APIs.
49. [edge cases] Are zero/negative gold, remote/unnamed/dead targets, scene-shout, and stale snapshots handled by one fail-closed gate? A: Compute eligibility and return typed denial before UI enable or mutation.
50. [simpler alternatives] If the gold API is not proven, is the simplest viable v1 to ship only the promise/request ledger? A: Yes, defer give-gold until native/game-data support is verified rather than inventing a second implementation.
VERDICT: REVISE
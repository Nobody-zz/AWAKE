1. [data/schema] Does the avatar identity schema distinguish `hero:` IDs, troop `npc:` IDs, and agent-indexed `:a{agentIndex}` instances? Recommended: define `awake.avatar.v1` with hero/troop/agent/fallback fields and key portraits by hero or troop identity only.
2. [data/schema] Can `CharacterCode`/`ImageIdentifier` be serialized without retaining TaleWorlds live objects? Recommended: store only kind, source ID, and fallback, then construct image identifiers from game state at open time.
3. [data/schema] Does the current `IAwakeContentRegistry` actually expose a tab registration API as C3 bullet 5 requires? Recommended: add a versioned `RegisterTab(IAwakeContactTabDefinition)` contract because the current public API has no tab surface.
4. [data/schema] Does the asset ban cover derivatives such as recolored textures, re-traced icons, and renamed brush copies, not only direct copies? Recommended: treat every derived visual/audio/animation asset as copied unless provenance is recorded.
5. [data/schema] Is there a schema for card/location/relationship/tab data, or will the VM hold ad hoc game-object fields? Recommended: add `awake.contact.v1` or upgrade messenger to v2 and register it in `AwakeStorageContract`.
6. [storage] Where do card snapshots, avatar keys, location, and tab state persist? Recommended: route all durable fields through `WorldStateStore` namespaces and commands, never ModuleData or custom JSON.
7. [storage] Does extending the messenger UI imply a new persisted schema beyond `awake.messenger.v1`? Recommended: plan `awake.messenger.v2` or a separate contact namespace with migration before adding card/avatar metadata.
8. [storage] Is the 512KB `StorageValueMaximumBytes` cap recalculated after adding avatar fallback labels, card summaries, and tab metadata? Recommended: enforce per-line, per-contact, and whole-document UTF-8 byte budgets and test the worst case.
9. [storage] Are avatar/card caches persisted, risking stale or oversized storage? Recommended: persist only stable IDs; keep image handles and display snapshots in memory and clear them on campaign lifecycle.
10. [storage] Are content-pack tab registrations idempotent and deterministic across module load order? Recommended: register from manifests at startup with unique IDs, explicit sort order, and duplicate-ID rejection.
11. [UI] Can the existing 1280x760 `AwakeMessenger.xml` host three columns, avatar widgets, and tabs without clipping at 1366x768 and 1280x720? Recommended: redesign with responsive minimum widths and screenshot/regression tests at both resolutions.
12. [UI] Does extending `AwakeMessengerOverlay/VM/Prefab` keep scene T/Y as a lightweight overlay or force all scene dialogue into the full hub? Recommended: keep the scene overlay for T/Y and route both entry points through the coordinator without a third hub overlay.
13. [UI] Can `ImageWidget` render `CharacterImageIdentifier`/`ImageIdentifier`, and what exactly is the unnamed-NPC fallback? Recommended: bind `ImageWidget` to the image identifier and use an AWAKE-owned generic placeholder, verified by UI smoke test.
14. [UI] Are content-pack tabs bounded and localized so many packs cannot overflow the panel? Recommended: cap visible tabs, sort deterministically, and hide packs that are absent or disabled.
15. [UI] Do long CJK names, unread counts, avatar rows, and card text stay within fixed row heights without clipping? Recommended: use `CanBreakWords`, clipped scroll panels, truncation limits, and CJK layout verification.
16. [UI] Does the revised correction add an automated check for Alice/AF names, prefab IDs, brush names, or layout constants in AWAKE XML/VM code? Recommended: add a lint rule rejecting those identifiers so the boundary is enforceable, not just documented.
17. [UI] Are tab/action buttons using AWAKE-native labels, icons, and tooltips instead of Alice button styles or interaction text? Recommended: use only AWAKE/game-native UI conventions and reject copied Alice button assets or strings.
18. [performance] Does `BuildContacts` still enumerate all `AliveHeroes` on every panel open after adding avatar/card work? Recommended: lazy-build and cap contact rows, caching stable metadata instead of rebuilding all heroes each open.
19. [performance] Are `CharacterCode`/`ImageIdentifier` objects cached per hero/troop rather than recreated per row or per frame? Recommended: cache for the panel lifetime and release on close, never create portrait handles per frame.
20. [performance] Does streaming chat still cause full list, card, and tab rebinding on each delta? Recommended: keep append-only stream updates and batch UI notifications so card/tab data changes only on explicit refresh.
21. [performance] Does content-pack tab registration trigger runtime reflection or asset scanning at open? Recommended: register statically at startup from validated manifests and avoid per-open scans.
22. [performance] Will the added card/tab/avatar layout materially increase VM allocation or UI hitching? Recommended: reuse existing list panels, avoid one overlay per tab, and cap card content with short snapshots.
23. [token] Are avatar, card, location, and tab fields excluded from NPC prompt inputs? Recommended: keep them UI-only and continue using controlled context providers and bounded memory summaries in `BuildPromptInputAsync`.
24. [token] Do content-pack tabs inject pack content into core NPC prompts by default? Recommended: no; tabs are UI-only unless the content passes the same policy/RAG gates as other enabled content.
25. [token] Are relationship/card summaries bounded before rendering and before any AI-facing use? Recommended: reuse `NpcMemorySelector.FormatTopK` and byte caps; never serialize full memory or event arrays.
26. [token] Does the revised correction still keep raw 200-line transcript out of AI prompts? Recommended: yes; raw history remains player-facing only and AI uses compressed memory/summary.
27. [token] Are localized tab/avatar strings excluded from token accounting? Recommended: keep UI localization outside prompt budget and only count actual AI input payloads.
28. [security] Can a content pack register a tab with arbitrary markup, sprite paths, or file paths? Recommended: validate/fail closed on unknown asset references and render labels as plain or sanitized text.
29. [security] Can a stored character string be used to construct arbitrary `ImageIdentifier`s or expose filesystem paths? Recommended: build identifiers only from validated hero/troop objects and return handles, never paths.
30. [security] Is there an allowlist for sprite/brush references so unknown or AF/Alice paths are rejected? Recommended: allow only native game brushes and AWAKE-owned assets, with build-time rejection of unknown paths.
31. [security] Are content-pack tab registrations permission-gated and ownership-checked to prevent one pack hijacking core tabs? Recommended: validate pack ownership, duplicate IDs, and registration failures; core tabs must remain present and fail closed.
32. [security] Is chat/letter text sanitized before binding to `RichTextWidget`? Recommended: render as plain text or sanitize markup so AI/content-pack strings cannot inject rich text into the shared hub.
33. [security] Does the content API require schema version, unique ID, and validation before a pack can add UI? Recommended: require schema version and unique ID and make registration return success/failure deterministically.
34. [validation] What happens when a hero/troop has no `CharacterObject` or the native portrait API throws? Recommended: catch per-contact failures and fall back to the generic AWAKE placeholder without blocking the hub.
35. [validation] Are duplicate hero/troop/agent contacts canonicalized before avatar caching and rendering? Recommended: deduplicate by stable hero/troop identity and show one canonical row rather than one row per agent index.
36. [validation] Are dead, captured, prisoner, or out-of-range contacts still selectable and given avatars? Recommended: revalidate at open/tick and disable actions while preserving history and showing expired/remote state.
37. [validation] Are card fields, tab definitions, and avatar fallbacks normalized on load without crashing on corrupt JSON? Recommended: coerce, truncate, and default invalid fields while logging corruption, matching existing storage normalization.
38. [validation] Does the revision specify acceptance evidence proving no Alice/AF asset or implementation reuse? Recommended: add asset manifest/hash lint, source searches, and SdkSmoke checks; otherwise the guarantee is not verifiable.
39. [save/load] Are avatar/card caches cleared on campaign reset and save load so stale portraits cannot leak across saves? Recommended: clear in-memory caches in `ResetCampaignState` and rebuild from current campaign state.
40. [save/load] Can old `awake.messenger.v1` saves load after the UI extension without losing chat history? Recommended: keep v1 loading or migration to v2 and add a save/load regression test.
41. [save/load] Are new contact/card fields persisted, or reconstructed from current game state? Recommended: persist only stable IDs and durable summaries; reconstruct location/status/portrait from current game state at open.
42. [save/load] If a content pack is absent on load, do its tabs disappear without corrupting saved contact/history state? Recommended: treat tabs as runtime registrations and keep saved data intact, hiding pack-specific rows gracefully.
43. [compatibility] Does extending `AwakeMessengerOverlay/VM/Prefab` conflict with the existing `NpcDialogueOverlay` and `WorldEventInboxOverlay` tick/close lifecycle? Recommended: route through `AwakeDialogueSessionCoordinator` and keep explicit lifecycle ownership without a parallel hub overlay.
44. [compatibility] Does the revised correction align with `AWAKE-ContactPanel-Concept-20260816.md`, which still says Phase A creates `AwakeContactHubOverlay`? Recommended: update the concept to remove or supersede the new overlay; contradictory documents are a material flaw.
45. [compatibility] Does the hub guard against unsupported modes such as main menu, battle, or multiplayer before constructing native portrait/tableau objects? Recommended: guard to single-player campaign and fail closed before building contacts or avatars.
46. [compatibility] Are content-pack tabs default-hidden when no pack is loaded, keeping the base panel content-neutral? Recommended: yes; the default panel must contain only core tabs and no pack-specific labels or behavior.
47. [edge] What is the avatar for a named troop NPC that also appears as an unnamed agent in the same scene? Recommended: use troop `CharacterObject` identity as the canonical avatar and never duplicate by agent index.
48. [edge] What happens when two content packs register the same tab ID or localized key? Recommended: reject duplicate tab IDs and prefix string keys with the pack ID, logging a deterministic registration error.
49. [simpler] Is a three-column card UI required for Phase A, or can the current two-column messenger be extended with a small bounded card summary first? Recommended: keep Phase A two-column plus a small card area and defer tabs/letters until consumers and tests exist.
50. [simpler] Is a content-pack tab API needed now, or should Phase A define only the contract and defer dynamic tab rendering until a real pack consumes it? Recommended: define the versioned API now but implement no dynamic tabs yet; the revised text overstates readiness without that API.
VERDICT: REVISE